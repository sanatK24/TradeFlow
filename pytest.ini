[pytest]
testpaths = tests
markers =
    unit: Unit tests for core business logic (matching engine, bond math)
    api: API integration tests using FastAPI TestClient
    e2e: End-to-end UI tests using Playwright
    contract: Live contract validation tests using requests and Pydantic
addopts = -v --tb=short
asyncio_mode = auto
